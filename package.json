{
  "name": "invincible-me",
  "version": "1.0.0",
  "description": "Invincible Me - Identity tracking app for university students",
  "private": true,
  "scripts": {
    "deploy": "cd client && VITE_API_BASE=https://invincible-me.onrender.com/api npm run build && cd .. && npx gh-pages -d client/dist"
  }
}

